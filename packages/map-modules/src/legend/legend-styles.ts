/** Shared surface style for legend overlays. */
export const glass = {
  background: "rgba(12,16,20,0.8)",
  backdropFilter: "blur(12px)",
  WebkitBackdropFilter: "blur(12px)",
  border: "1px solid rgba(255,255,255,0.05)",
  borderRadius: 8,
} as const;
